(function chai(){      // it is and named IIFI because it have name of funcion
    console.log('DB CONNECTEC');
})();

// suing arow  function inside iffi

((name)=>{        // it is unname IIFI because it is not have function name.
 console.log('DB CONNECTED TWO ${name}');
})('Rajesh')